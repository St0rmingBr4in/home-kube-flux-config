apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: jackett-flux-jackett-flux-kustomization-kustomization-c82366d0
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 10m0s
  path: ./clusters/home-lab/cdk8s_definitions/dist/prod-media/jackett
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
