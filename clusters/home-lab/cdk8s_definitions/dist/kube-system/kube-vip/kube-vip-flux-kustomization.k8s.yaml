apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kube-vip-flu-kube-vip-flux-kustomization-kustomization-c89944e4
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 10m0s
  path: ./clusters/home-lab/cdk8s_definitions/dist/kube-system/kube-vip
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
