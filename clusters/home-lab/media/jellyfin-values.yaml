---
image:
  tag: latest
ingress:
  main:
    enabled: true
    hosts:
      - host: jellyfin.ilovecara.ml
        paths:
          - path: /
persistence:
  config:
    enabled: true
    existingClaim: jellyfin-config
  media:
    enabled: true
    existingClaim: media-library
