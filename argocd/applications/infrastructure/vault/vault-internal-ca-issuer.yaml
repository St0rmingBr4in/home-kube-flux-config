---
# Vault-specific CA ClusterIssuer (uses the CA certificate created by Vault)
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-internal-ca
spec:
  ca:
    secretName: vault-internal-ca-secret
