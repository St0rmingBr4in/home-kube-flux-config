---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
# Add any additional resources here like:
# - pihole-secret.yaml (if admin password needed)
# - pihole-configmap.yaml (if custom DNS entries needed)
