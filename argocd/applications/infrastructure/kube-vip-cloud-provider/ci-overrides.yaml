# CI Override: Kube-VIP Cloud Provider optimized for testing
# Reduced resources for CI
resources:
  limits:
    cpu: 50m
    memory: 64Mi
  requests:
    cpu: 25m
    memory: 32Mi

# Single replica
replicaCount: 1

# Disable leader election for CI
args:
  - --leader-elect=false
