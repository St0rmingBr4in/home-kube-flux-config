---
apiVersion: v1
kind: Service
metadata:
  name: ats-backend
  namespace: prod-ats
spec:
  selector:
    app.kubernetes.io/name: ats-backend
  ports:
    - port: 8080
      targetPort: 8080
      name: http
