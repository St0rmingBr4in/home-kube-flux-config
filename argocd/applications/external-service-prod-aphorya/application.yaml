apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-service-prod-aphorya
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://charts.t3n.io
      chart: external-service
      targetRevision: 1.1.0
      helm:
        valueFiles:
          - $values/argocd/applications/external-service-prod-aphorya/external-service-values.yaml
    - repoURL: https://github.com/St0rmingBr4in/home-kube-flux-config
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: prod-aphorya
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 10s
        maxDuration: 5m
        factor: 2
