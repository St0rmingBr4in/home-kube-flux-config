---
server:
  resources:
    requests:
      memory: 150Mi
      cpu: 100m
    limits:
      memory: 150Mi
  ingress:
    enabled: true
    hosts:
      - host: vault.st0rmingbr4in.com
        paths: ["/"]
    tls:
      - secretName: st0rmingbr4in.com-cert
        hosts:
          - vault.st0rmingbr4in.com

ui:
  enabled: true

injector:
  enabled: false
