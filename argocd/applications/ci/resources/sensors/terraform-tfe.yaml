---
# TFE module only needs the Terraform Cloud token — no module-specific secrets.
# vault-secret-path points to a path that exists but may be empty (or unused fields are ignored).
apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: ci-terraform-tfe
  namespace: argo
spec:
  template:
    serviceAccountName: argo-ci
  dependencies:
    - name: push
      eventSourceName: github
      eventName: push
      filters:
        data:
          - path: body.ref
            type: string
            value:
              - "refs/heads/master"
  triggers:
    - template:
        name: trigger
        k8s:
          operation: create
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: ci-terraform-tfe-
                namespace: argo
              spec:
                workflowTemplateRef:
                  name: ci-terraform
                arguments:
                  parameters:
                    - name: git-ref
                      value: "refs/heads/master"
                    - name: module
                      value: tfe
                    - name: path-filter
                      value: "terraform/tfe"
                    - name: vault-secret-path
                      value: "secret/ci/terraform-cloud"
                    - name: git-sha
                      value: ""
                    - name: git-author-email
                      value: ""
          parameters:
            - src:
                dependencyName: push
                dataKey: body.ref
              dest: spec.arguments.parameters.0.value
            - src:
                dependencyName: push
                dataKey: body.after
              dest: spec.arguments.parameters.4.value
            - src:
                dependencyName: push
                dataKey: body.head_commit.author.email
              dest: spec.arguments.parameters.5.value
      retryStrategy:
        steps: 3
