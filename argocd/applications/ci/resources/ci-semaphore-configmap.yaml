---
# Argo Workflows semaphore: limits concurrent CI runs to 3.
# All workflow templates reference this configmap via synchronization.semaphore.
# To allow more/fewer concurrent runs, increase/decrease the value.
apiVersion: v1
kind: ConfigMap
metadata:
  name: ci-semaphore
  namespace: argo
data:
  ci: "3"
