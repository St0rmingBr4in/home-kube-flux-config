# kube-vip DaemonSet Values
# TEMPORARILY DISABLED due to network conflicts

# Disable the DaemonSet to prevent network interface conflicts
daemonset:
  enabled: false

# kube-vip configuration
config:
  address: ""

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
