daemonset:
  enabled: true

config:
  address: "192.168.72.2"

env:
  # Enable both control plane and LoadBalancer service watching
  cp_enable: "true"
  svc_enable: "true"

  # Disable IPVS, use Layer 2 mode
  lb_enable: "false"
