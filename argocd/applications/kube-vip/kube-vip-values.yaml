daemonset:
  enabled: true

config:
  address: "192.168.72.2"

env:
  # LoadBalancer configuration for API server
  lb_enable: "true"
  lb_port: "6443"
  vip_arp: "true"
  svc_enable: "true"
  cp_enable: "true"
