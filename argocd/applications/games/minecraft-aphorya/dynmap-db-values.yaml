---
primary:
  resources:
    limits:
      memory: 256Mi
    requests:
      memory: 256Mi # Memory webhook will ensure this equals limit for guaranteed QoS
      cpu: 250m
  persistence:
    enabled: false
auth:
  existingSecret: "dynmap-db-postgresql"
